---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: karpenter
  namespace: flux-system
spec:
  chart:
    spec:
      chart: karpenter
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: karpenter
        namespace: flux-system
      version: 0.36.2
  install:
    createNamespace: true
  interval: 1m0s
  releaseName: karpenter
  targetNamespace: karpenter
  values:
    controller:
      resources:
        limits:
          cpu: 1
          memory: 1Gi
        requests:
          cpu: 1
          memory: 1Gi
    serviceAccount:
      annotations:
        eks.amazonaws.com/role-arn: arn:aws:iam::054318140392:role/Karpenter-prow-canary-cluster-20240527081538530000000004
    settings:
      clusterName: prow-canary-cluster
      interruptionQueue: Karpenter-prow-canary-cluster
